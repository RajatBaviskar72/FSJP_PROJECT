# Database Configuration
# Default XAMPP MySQL settings (leave DB_PASSWORD blank if no password set in XAMPP)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=fsjp_project
DB_PORT=3306

# Server Port
PORT=3000
